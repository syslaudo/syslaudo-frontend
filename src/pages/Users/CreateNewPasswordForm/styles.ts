import styled from 'styled-components';

export const StyledContainer = styled.div`
  display: flex;
  flex-direction: column;

  > a {
    margin: 0 auto;
  }

  .svg {
    max-width: 80%;
    height: 8rem;
    margin: 2rem auto;
  }
`;
